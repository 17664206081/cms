group 'com.fzy'
version '1.0-SNAPSHOT'

bootRepackage {
    enabled = false
}

dependencies {
    compile project(":cms-dao")
    compile project(":cms-utils")
    compile project(":cms-exception")
}
